<?xml version="1.0" encoding="utf-8"?>
<!--
  The picker gets its own layout rather than reusing the drawer: the drawer is paged
  sideways, which is right while driving and wrong for scanning an alphabetical list
  while parked.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="88dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="40dp"
        android:paddingEnd="40dp">

        <TextView
            android:id="@+id/back"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:text="Cancel"
            android:textColor="@color/ink_soft"
            android:textSize="20dp"
            android:clickable="true"
            android:focusable="true"
            android:paddingEnd="24dp" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Choose an app"
            android:textColor="@color/ink"
            android:textSize="24dp"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/muted"
            android:textSize="15dp"
            android:fontFamily="monospace" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/hairline" />

    <GridView
        android:id="@+id/grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:numColumns="5"
        android:paddingStart="40dp"
        android:paddingEnd="40dp"
        android:paddingTop="26dp"
        android:paddingBottom="26dp"
        android:clipToPadding="false"
        android:verticalSpacing="22dp"
        android:horizontalSpacing="18dp"
        android:scrollbars="none" />
</LinearLayout>
